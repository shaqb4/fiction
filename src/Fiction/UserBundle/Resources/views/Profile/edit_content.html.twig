{% trans_default_domain 'FOSUserBundle' %}

<form role="form" action="{{ path('fos_user_profile_edit') }}" {{ form_enctype(form) }} method="POST" class="fos_user_profile_edit">
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary" value="{{ 'profile.edit.submit'|trans }}">
    	{{ 'profile.edit.submit'|trans }}
    </button>
</form>
