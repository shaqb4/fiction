{% trans_default_domain 'FOSUserBundle' %}

<form role="form" action="{{ path('fos_user_change_password') }}" {{ form_enctype(form) }} method="POST" class="fos_user_change_password">
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary" value="{{ 'change_password.submit'|trans }}">
    	{{ 'change_password.submit'|trans }}
    </button>
</form>
